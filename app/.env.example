# Basirah — AI Configuration (Development Only)
#
# Copy this file to `.env.local` and fill in your values.
# `.env.local` is gitignored — never commit real keys.
#
# ⚠️ SECURITY NOTE:
# EXPO_PUBLIC_* variables are embedded in the client bundle and are
# visible to anyone who inspects the app. This setup is for DEVELOPMENT
# ONLY. A secure backend/API layer must replace this before production.

# Required: Gemini API key from Google AI Studio
# https://aistudio.google.com/apikey
EXPO_PUBLIC_GEMINI_API_KEY= AQ.Ab8RN6JjEZMGxbIYeiAmHgMyTCABJwS0khUufGL20gq40tSPOQ

# Optional: override the default Gemini model
# Default: gemini-2.0-flash
# EXPO_PUBLIC_GEMINI_MODEL=gemini-2.0-flash